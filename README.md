# Sirus Teknologi Official Website

## Quick Start

1. create `.env` file and add necessary configuration, you can look up the configuration sample on `.env.defaults` file

1. install dependency

    ```bash
    npm install
    ```

1. build and run

    ```bash
    npm run build
    npm start
    ```
